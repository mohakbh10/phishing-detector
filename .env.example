# Never commit your real .env file.
GOOGLE_SAFE_BROWSING_API_KEY=replace_with_your_key

# Comma-separated frontend origins. Keep this explicit; do not use *.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://your-frontend.example.com
